import { startDrawingLayout, startAnimating } from "./module/initializer.js";

// Setting Configurations (Currently Using Default Config so no need to set explicitly)
// setConfiguration()
startDrawingLayout();
startAnimating();
